name: transform-test
description: Constant values capitalised (generator + inline transformer)

steps:
  - id: words
    generator:
      name: scarfolder.generators.util.Constant
      args:
        value: hello
        count: 3
    transformer: scarfolder.transformers.text.capitalize_first  # values auto-injected
